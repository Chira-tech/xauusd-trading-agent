pyyaml>=6.0
numpy>=1.26
# optional -- only needed if you set data_source: yfinance in goal.yaml
# yfinance>=0.2.40
# optional -- only needed for `--llm --backend api` (the default --backend cli
# uses the `claude` CLI and needs nothing here)
# anthropic>=0.40
